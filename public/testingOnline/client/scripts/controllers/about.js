'use strict';

angular.module('quizApp')
    .controller('AboutCtrl', function ($scope, Auth, $http, $location, $window) {

        function init() {

        } 
        init();
        this.awesomeThings = [
            'HTML5 Boilerplate',
            'AngularJS',
            'Karma'
        ];
    });
